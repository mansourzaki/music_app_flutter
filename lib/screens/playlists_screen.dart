import 'package:flutter/material.dart';

class PlayListsScreen extends StatelessWidget {
  PlayListsScreen({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('PlayLists'),);
  }
}